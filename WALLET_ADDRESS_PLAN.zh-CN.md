# USDT / TRC20 每单独立地址方案

目标：每个短时订单使用不同收款地址，减少地址复用，方便订单匹配，也降低未知转账和可疑资金混入的处理难度。

## 参考成熟方案

成熟支付系统常见做法是“每张 invoice 生成唯一地址”：

- BTCPay Server 会为每张 invoice 从商户钱包生成一个唯一地址，并跟踪链上付款状态。
- Bitcart 文档说明使用 xpub 可以在每次创建 invoice 时生成新地址。
- SHKeeper 的说明也强调客户会收到二维码或唯一地址，付款后通过 callback 通知商户。

对本站的启发是：不要长期公开一个固定地址；订单创建时分配短时有效的独立地址，支付完成后通过链上监听或回调进入审核。

## TRC20 的推荐实现

更安全的路径是离线地址池：

1. 在离线设备或硬件钱包上生成一批 TRON 收款地址。
2. 将私钥离线保存，不放到服务器。
3. 只把地址列表导入 `/opt/donate-gateway/config/address-pool.trc20.json`。
4. 创建订单时从地址池分配一个未占用地址。
5. 订单过期后，地址进入冷却期，不立刻复用。
6. 后台只监听这些地址的 USDT / TRC20 入账。
7. 审核通过后，由管理后台发起金额型提现请求，独立钱包服务执行归集到 Binance 并回调后端。

这种方式比服务器热钱包更笨，但更不容易因为私钥泄露造成资金丢失。

## 自动创建地址与自动回收

可以自动化，但分两级。

### 推荐第一版：自动分配/回收地址池

服务端不生成私钥，只管理离线生成的地址池。

创建订单时：

- 从地址池中分配一个当前未被有效订单占用的地址。
- 将地址绑定到订单，订单有效期默认 30 分钟。
- 如果订单仍是 `created` 且已经过期，系统自动删除该未付款订单，释放地址进入后续分配。删除仅适用于未检测到付款的 `created` 订单；已检测付款、待审核、冻结、拒绝、接受、已归集订单必须保留审计记录。
- 已付款、审核中、冻结调查、已接受、已归集的订单地址不自动复用。

这满足“每单不同、短时有效、可回收”的运营需求，同时不把私钥放到服务器。

### 已支持但需谨慎：服务器自动生成真实钱包地址

如果服务器自动创建 TRON 地址，通常意味着服务器要持有私钥、种子、xprv 或可派生私钥的材料。一旦服务被入侵，收款地址和未来地址都可能失守。

当前实现允许本地服务器生成 TRON 收款地址，并把私钥加密保存为热钱包材料。它可以用于小额、短时订单，但必须配合最小余额策略、完整备份和恢复演练、私钥加密和密钥管理、提现前审核、事件审计和异常冻结。

## 地址池格式建议

```json
{
  "asset": "USDT",
  "network": "TRC20",
  "addresses": [
    {
      "address": "T...",
      "label": "trc20-order-0001",
      "status": "available"
    }
  ]
}
```

状态：

- `available`：可分配。
- `reserved`：已绑定未过期订单。
- `paid`：已检测到付款。
- `cooldown`：订单结束后冷却，不立即复用。
- `disabled`：不再使用。

## 订单有效期

建议：

- 默认 30 分钟。
- 过期未付款：先进入观察期，默认继续保留 1440 分钟并继续扫描链上付款。
- 观察期内仍无付款：删除未付款订单，地址进入后续可分配状态。
- 观察期内发现付款：不删除订单；若金额不匹配，进入 `frozen_review` 并保留地址、来源地址、tx hash 和审核记录。

## 服务器生成地址的边界

不建议长期让公网服务器承担完整热钱包职责。真实方案应使用以下任一方式：

- 离线或硬件钱包预生成地址池，只把公钥地址写入服务器。
- 使用受控钱包服务或自托管冷/温钱包服务生成地址，服务器只拿到可收款地址，不保存私钥。
- 如必须自动生成，也应在独立钱包服务中完成，私钥加密保存并隔离权限；支付网关只调用“分配地址”接口。

当前实现会拒绝 `TEST_ONLY_` 地址和非 TRON 地址，避免把测试地址展示给真实用户。

## 来源地址

创建订单时通常还没有链上交易，因此没有来源地址。来源地址必须在付款检测后从链上交易或支付服务 webhook 中写入：

- `senderAddress` / `from`：付款来源地址。
- `txHash`：链上交易哈希。
- `screening`：AML/制裁/风险评分结果。

只有记录到来源地址并完成审核后，订单才允许被标记为合法资金并进入归集流程。
- 付款后：订单进入 `pending_review` 或 `frozen_review`，地址不再分配给新订单。
- 金额不符也是“已付款事件”，必须保留订单和地址；不得因为订单过期或金额异常而自动释放地址。

## 为什么不把 Binance 充值地址直接公开

公开固定地址会导致任何来源都能直接入账，无法阻止黑钱或错误转账进入 Binance。本站应先把外部收款留在可控地址池，完成订单匹配和风险记录后，再通过提现请求交给钱包服务归集到 Binance。

## 归集到 Binance 的硬规则

只有同时满足以下条件，才允许归集：

- 订单状态是 `accepted`。
- 管理员资金标签是 `legal`。
- 已记录原始 tx hash、付款地址、收款地址、金额、网络和审核备注。
- 没有制裁、诈骗、混币、盗窃、勒索、暗网等风险命中。

`unknown`、`suspicious`、`illegal`、`frozen_review`、`rejected` 都不能归集到 Binance。

## 后续可升级路径

如果后续需要更自动化：

- 使用只读派生能力生成地址，服务器只持有 xpub/watch-only 信息，不持有私钥。
- 接入成熟自托管支付网关，例如 SHKeeper 或 Bitcart。
- 接入链上风险筛查服务后，再决定是否允许半自动归集。

上线前仍要确认 TRON 钱包、地址生成方式、私钥备份和归集流程，避免因地址派生路径错误或私钥丢失造成资金不可恢复。
